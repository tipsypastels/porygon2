import { GuildHandler } from 'lib/lib/event';

const boundRoleHandler: GuildHandler = ({ em }) => {
  // em.on('guildMemberUpdate', (from, to) => {
  //   //
  // });
};

export default boundRoleHandler;
