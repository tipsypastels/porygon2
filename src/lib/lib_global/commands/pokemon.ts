// import { search, Pokemon } from '@tipsypastels/pokemon';
import { Command } from 'interaction/command';

// eslint-disable-next-line
// const { search, Pokemon } = require('@tipsypastels/pokemon');

const pokemon: Command = async ({ embed }) => {
  // console.log(Pokemon);
  // const mon = await search('bulbasaur');
  // await embed.setTitle(mon?.name).reply();
};

pokemon.description = 'asdf';

export default pokemon;

export const __SKIP_FILE__ = 1;
